@model IEnumerable<BibliotecaWebApplication.Models.Revista>

@{
    ViewData["Title"] = "Revistas";
}

<div class="container p-4">
    <h2 class="text-center text-uppercase mb-4">Revistas</h2>

    <div class="d-flex justify-content-between mb-4">
        <p>
            <a asp-action="Create" class="btn btn-custom btn-create"><i class="fas fa-plus"></i> Crear Revista</a>
        </p>
    </div>

    <table id="dataTable" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Numero)</th>
                <th>@Html.DisplayNameFor(model => model.Nombre)</th>
                <th>@Html.DisplayNameFor(model => model.FechaPublicacion)</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Numero)</td>
                    <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                    <td>@Html.DisplayFor(modelItem => item.FechaPublicacion)</td>
                    <td class="text-center">
                        <a asp-action="Edit" asp-route-id="@item.RevistaId" class="btn btn-custom btn-edit btn-sm"><i class="fas fa-edit"></i> Editar</a>
                        <a asp-action="Details" asp-route-id="@item.RevistaId" class="btn btn-custom btn-details btn-sm"><i class="fas fa-info-circle"></i> Detalles</a>
                        <a asp-action="Delete" asp-route-id="@item.RevistaId" class="btn btn-custom btn-delete btn-sm"><i class="fas fa-trash-alt"></i> Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#dataTable').DataTable({
                "language": {
                    "lengthMenu": "Mostrar _MENU_ entradas por página",
                    "zeroRecords": "No se encontraron resultados",
                    "info": "Mostrando _START_ a _END_ de _TOTAL_ entradas",
                    "infoEmpty": "Mostrando 0 a 0 de 0 entradas",
                    "infoFiltered": "(filtrado de _MAX_ entradas totales)",
                    "search": "Buscar:",
                    "paginate": {
                        "first": "Primero",
                        "last": "Último",
                        "next": "Siguiente",
                        "previous": "Anterior"
                    }
                }
            });
        });
    </script>
}

<style>
    .btn-custom {
        padding: 10px 20px;
        border-radius: 25px;
        font-weight: bold;
        transition: background-color 0.3s ease, box-shadow 0.3s ease;
    }

    .btn-create {
        background-color: #6c757d; /* Gris claro */
        border: none;
        color: white;
    }

        .btn-create:hover {
            background-color: #5a6268; /* Gris oscuro */
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
        }

    .btn-edit {
        background-color: #17a2b8; /* Azul suave */
        border: none;
        color: white;
    }

        .btn-edit:hover {
            background-color: #138496; /* Azul oscuro */
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
        }

    .btn-details {
        background-color: #ffc107; /* Amarillo suave */
        border: none;
        color: black;
    }

        .btn-details:hover {
            background-color: #e0a800; /* Amarillo oscuro */
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
        }

    .btn-delete {
        background-color: #dc3545; /* Rojo suave */
        border: none;
        color: white;
    }

        .btn-delete:hover {
            background-color: #c82333; /* Rojo oscuro */
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
        }

    .container {
        max-width: 1200px;
        margin: auto;
    }

    h2 {
        font-size: 2rem;
        font-weight: 700;
        letter-spacing: 1px;
    }

    .table {
        border-collapse: separate;
        border-spacing: 0;
        border: 1px solid #ced4da;
    }

        .table th, .table td {
            vertical-align: middle;
            border: 1px solid #ced4da;
        }

        .table th {
            background-color: #f8f9fa;
            color: #495057;
        }

        .table td {
            background-color: #ffffff;
            color: #212529;
        }
</style>
